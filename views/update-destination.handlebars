
<h1>Update {{destination.city}} {{destination.country}}:</h1>
<form id="update-destination" action="/destination" method="post" onsubmit="return checkEmpty('city', 'country')">
    City: <input type="text" name="city" id="city" value="{{destination.city}}"><br>
    Country: <input type="text" name="country" id="country" value="{{destination.country}}"><br>
</form>
<button onclick="updateRow('destination', {{destination.id}})">Update</button>